export interface AbdomenState {
  umbilicus: boolean;
  suprapubic: boolean;
  ruq: boolean;
  rlq: boolean;
  luq: boolean;
  llq: boolean;
  epigastrium: boolean;
  selectAll: boolean;
}

export interface FingerState {
  mcp: boolean;
  dip: boolean;
  pip: boolean;
  selectAll: boolean;
}
